# 使用内部 GitLab 管理项目仓库

平台使用不直接暴露给用户的内部 GitLab 保存批准的设计产物，每个平台项目对应一个独立仓库。用户和 Agent 均不直接获得写权限，只有受限于专用 Group 的平台服务账号可以在 PM 批准后写入保护分支；业务数据库保存项目状态、仓库标识、commit SHA 和产物索引，界面通过平台后端提供预览、差异比较和下载能力。原始 PDF、图片等二进制附件不进入 Git 仓库。
