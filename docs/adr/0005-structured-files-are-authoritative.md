# 结构化文件是产物权威源

每份设计产物以符合版本化 Schema 的结构化 YAML 文件表达完整语义，Markdown 等人类可读文件由结构化内容确定性渲染并与其一起提交。Validator 和下游 Agent 只以结构化源文件为事实输入，渲染文件不允许独立编辑；产物自身记录 Schema 版本和内容哈希，稳定 Git 路径与阶段基线 Commit 确定源文件及渲染文件，不再建立独立 Manifest，从根本上避免机器内容与展示文档形成两份真相。
