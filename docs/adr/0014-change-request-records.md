# 变更请求分运行态与永久决议存储

处理中变更以 PostgreSQL 为权威，记录来源、目标、基线、状态、影响分析和应用进度；上传附件进入对象存储。变更批准后通过事务任务提交新产物和基线，只有 GitLab 写入成功并记录 commit SHA 后状态才成为 `APPLIED`，此前下游不得消费新基线。批准、拒绝和用户撤销三类终态都生成永久变更决议并提交项目仓库，因为明确决定不做的内容同样约束后续 PM 和 Agent。
