# 模块化单体与独立运行进程

V2 采用一个代码仓库和一个共享 Python 业务包，按项目、工作流、产物、需求、架构、数据库、接口、测试、变更、RFI 和评测等领域模块组织；API、Worker 和 Scheduler 使用同一镜像及同一套业务规则，通过不同入口独立部署和扩容。首发不拆微服务，以保留项目真相、基线和变更的清晰事务边界，同时避免把长时间 Graph 重新绑定到 API 请求进程。
