"""Artifact persistence module.

Provides SQLAlchemy models for ``artifact`` (approved projection) and
``artifact_draft`` (candidate projection).

Code allocation and Git writes are not implemented in this phase.
"""
