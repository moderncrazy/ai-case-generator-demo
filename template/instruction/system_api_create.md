## API 设计

> **填写说明**：以下内容由 PM 根据业务需求填写，用于指导后端开发设计API接口。

### 0. 业务背景
- **项目名称**：[项目名称]
- **项目类型**：[项目类型]
- **业务描述**：[描述业务场景]
- **目标用户**：[系统服务的目标用户]
- **使用场景**：[典型使用环境/设备]
- **核心价值**：[系统要解决的根本问题]
- **约束条件**：[关键技术/业务限制]

### 1. 全局规范
- **认证方式**：[如：JWT Token]
- **返回格式**：[统一返回格式，如：{code, data, message}]

### 2. 接口清单
| 接口描述 | 路径 | 方法 | 核心入参 | 核心出参 |
|---------|------|------|---------|---------|
| [如：创建订单] | [如：/api/orders] | [如：POST] | [如：user_id, items] | [如：order_id] |
| [如：查询订单] | [如：/api/orders/{id}] | [如：GET] | [如：order_id] | [如：订单详情] |

### 3. 业务规则
- **异步场景**：[如：AI生成接口返回任务ID，轮询获取结果]
- **级联操作**：[如：删除订单时级联删除子订单]
- **幂等要求**：[如：支付接口需保证幂等]

### 4. 错误处理
- **业务错误码**：[如：4001-资源不存在，4002-权限不足]
- **空值处理**：[如：空列表返回[]，空对象返回{}]

### 5. 性能要求
- **分页策略**：[如：每页最多100条]
- **QPS限制**：[如：搜索接口限制100次/秒]
