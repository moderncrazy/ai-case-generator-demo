```json
{
  "message": "给评审同学的留言，说明设计要点和重点关注的地方（简洁）",
  "module_content": "模块详细内容",
  "risks": [{"content": "风险描述", "propose": "建议方案"}],
  "unclear_points": [{"content": "不明确问题", "propose": "建议方案"}]
}
```

## ⚡️ 模块内容格式

```markdown
### X.X [模块名称]

#### 页面原型
<!-- 简洁 HTML -->

#### 模块概述 / 前置条件 / 外部依赖
[简洁描述]

#### 字段说明
| 字段名 | 类型 | 来源 | 必填 | 校验 | 默认值 |
|--------|------|------|------|------|--------|
| ... | ... | ... | ... | ... | ... |

#### 交互说明
[简洁描述]

#### 模块闭环
[简要描述]
```

## ⚡️ 规范要求

1. **HTML 原型:** 仅限 `table`, `tr`, `td`, `div`, `button`, `input`
2. **表格式思维:** 字段定义、校验、逻辑全部采用 Markdown 表格
3. **逻辑表达:** 统一使用 `[触发条件] -> [动作] -> [结果]` 格式
4. **闭环原则:** 必须包含"数据来源"与"状态终点"