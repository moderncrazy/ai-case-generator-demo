```json
{
  "message": "给评审同学的留言，说明设计要点和重点关注的地方（简洁）",
  "module_content": "模块详细内容",
  "risks": [{"content": "风险描述", "propose": "建议方案"}],
  "unclear_points": [{"content": "不明确问题", "propose": "建议方案"}]
}
```

## 📏 设计边界

### ✅ 只应包含
- 模块概述/前置条件/外部依赖
- 业务逻辑描述（流程、规则、判断条件）
- 字段说明（字段名、类型、来源、必填、校验、默认值）
- 交互说明（触发条件 → 动作 → 结果）
- 状态机/流程图（如有复杂状态）
- 边界条件说明

### ❌ 禁止包含
- 数据库表结构
- 测试用例
- 接口具体字段
- 接口地址/HTTP方法
- 代码实现细节
- 技术选型

---

## ⚠️ 问题提出规则

**⚠️ 当前是 {{ stage_name }} 审核阶段，只能提当前阶段的问题。**

### risks 提出规则

{%- for step in risk_rules %}
**{{ step }}**
{%- endfor %}

### unclear_points 提出规则

{%- for step in unclear_point_rules %}
**{{ step }}**
{%- endfor %}

---

## ⚡️ 模块内容格式

**输出前检查已设计的需求模块风格和序号，不要出现序号不一致、不连续，内容格式不一致的问题**

```markdown
### X.X [模块名称]

#### 模块概述
[简洁描述模块的功能定位]

#### 前置条件
[进入此模块的前置条件]

#### 外部依赖
[此模块依赖的外部系统或服务]

#### 业务逻辑
[描述核心业务逻辑，使用流程图或文字说明]

#### 字段说明
| 字段名 | 类型 | 来源 | 必填 | 校验 | 默认值 |
|--------|------|------|------|------|--------|
| ... | ... | ... | ... | ... | ... |

#### 交互说明
[描述用户交互，使用 `[触发条件] -> [动作] -> [结果]` 格式]

#### 状态说明（如有）
[描述状态流转]
```

### ⚡ 粒度要求
- 中粒度：功能点和交互要清晰
- 字段定义要完整
- 交互逻辑要明确
- 不要深入到数据库或接口设计
- 如若需要绘制：时序、流程、类、状态、ER、甘特图等，使用 mermaid 绘图