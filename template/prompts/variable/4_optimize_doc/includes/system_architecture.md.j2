```json
{
  "message": "给评审同学的留言，说明架构设计的要点和重点关注的地方（简洁）",
  "system_architecture_content": "架构设计内容（Markdown格式）",
  "risks": [{"content": "给客户提出的风险描述", "propose": "建议方案"}],
  "unclear_points": [{"content": "提给客户的不明确问题", "propose": "建议方案"}]
}
```

## 📏 设计边界

### ✅ 只应包含
- 系统架构图（Mermaid 绘制，描述整体分层结构）
- 技术选型（语言、框架、中间件、数据库）
- 部署架构（测试/生产环境、容器化方案）
- 模块职责划分（各服务的职责和边界）
- 数据流向和模块依赖关系

### ❌ 禁止包含
- 业务逻辑流程
- 性能优化建议（如缓存策略）
- 数据库表结构
- 测试用例
- 接口具体字段
- 接口地址/HTTP方法
- 代码实现细节
- 技术选型

---

## ⚠️ 问题提出规则

**⚠️ 当前是 {{ stage_name }} 审核阶段，只能提当前阶段的问题。**

### risks 提出规则

{%- for step in risk_rules %}
**{{ step }}**
{%- endfor %}

### unclear_points 提出规则

{%- for step in unclear_point_rules %}
**{{ step }}**
{%- endfor %}

---

## ⚡️ 架构设计规范

### 1. 系统架构图
```mermaid
graph TB
    A[客户端] --> B[网关]
    B --> C[服务A]
    B --> D[服务B]
    C --> E[(数据库)]
```

### 2. 技术选型
| 组件 | 技术栈 | 说明 |
|------|--------|------|
| 前端 | React/Vue | 框架选型 |
| 后端 | Python/Go | 语言选型 |
| 数据库 | MySQL/PostgreSQL | 主从架构 |
| 缓存 | Redis | 哨兵模式 |

### 3. 部署架构
| 环境 | 部署方式 | 说明 |
|------|----------|------|
| 测试 | Docker | 本地测试 |
| 生产 | K8s | 集群部署 |

### 4. 核心模块说明
| 模块 | 职责 | 技术选型 |
|------|------|----------|
| 用户模块 | 用户管理 | Flask + JWT |
| 订单模块 | 订单处理 | FastAPI + RabbitMQ |

## 架构闭环
[简要描述架构的数据流向和模块依赖]

### ⚡ 粒度要求
- 中粒度：描述架构层级和技术选型
- 关注模块划分和依赖关系
- 不需要深入到接口或表结构
- 示例：描述"订单服务"而不需要描述"订单服务有哪几个API"
- 如若需要绘制：时序、流程、类、状态、ER、甘特图等，使用 mermaid 绘图