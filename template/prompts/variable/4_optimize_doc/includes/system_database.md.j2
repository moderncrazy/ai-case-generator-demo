```json
{
  "message": "给评审同学的留言，说明数据库设计的要点和重点关注的地方（简洁）",
  "system_database_content": "数据库设计内容（Markdown格式，通常包含SQL语句）"
}
```

## 📏 设计边界

### ✅ 只应包含
- ER图（实体关系图）
- 表结构定义（表名、字段、类型、约束）
- 索引设计（主键、唯一索引、普通索引）
- 表间关系（外键、关联）
- 数据字典（字段说明）

### ❌ 禁止包含
- 系统架构
- 模块职责划分
- 代码实现细节
- 业务逻辑流程
- 测试用例
- 接口具体字段
- 接口地址/HTTP方法
- 代码实现细节
- 技术选型

---

## ⚡️ 数据库设计规范

### 1. 表结构设计
```sql
CREATE TABLE `user` (
  `id` bigint(20) NOT NULL AUTO_INCREMENT COMMENT '用户ID',
  `username` varchar(50) NOT NULL COMMENT '用户名',
  `email` varchar(100) NOT NULL COMMENT '邮箱',
  `created_at` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `updated_at` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_username` (`username`),
  KEY `idx_email` (`email`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户表';
```

### 2. 索引设计
- 主键使用自增ID或UUID
- 唯一索引使用 UNIQUE KEY
- 频繁查询字段添加普通索引
- 避免过多索引影响写入性能

### 3. 命名规范
| 类型 | 规范 | 示例 |
|------|------|------|
| 表名 | 小写下划线 | user_info |
| 字段名 | 小写下划线 | user_name |
| 索引名 | idx_表名_字段 | idx_user_email |
| 外键名 | fk_表名_字段 | fk_order_user |

### 4. 字段类型选择
| 场景 | 推荐类型 |
|------|----------|
| 主键ID | bigint(20) |
| 状态字段 | tinyint(4) |
| 时间字段 | datetime |
| 金额字段 | decimal(10,2) |
| 长文本 | text |

### ⚡ 粒度要求
- 细粒度：表结构要详细，包含每个字段的类型和约束
- 不需要深入到SQL优化细节
- 关注表关系和数据字典
- 如若需要绘制：时序、流程、类、状态、ER、甘特图等，使用 mermaid 绘图
- 示例：描述"用户表有username字段，类型varchar(50)，不能为空"而不需要描述"如何优化这个查询"
