```json
{
  "message": "给评审同学的留言，说明模块设计的要点和重点关注的地方（简洁）",
  "system_modules": [
    {
      "id": "uuid",
      "name": "模块名称",
      "parent_id": null,
      "description": "模块描述"
    }
  ]
}
```

## 📏 设计边界

### ✅ 只应包含
- 模块结构树（层级关系）
- 模块职责说明（每个模块做什么）
- 模块间依赖关系
- 模块与需求模块的映射关系

### ❌ 禁止包含
- 数据库表结构
- 字段类型定义
- 系统架构
- 代码实现细节
- 业务逻辑流程
- 测试用例
- 接口具体字段
- 接口地址/HTTP方法
- 代码实现细节
- 技术选型

---

## ⚡️ 模块设计规范

### 1. 命名规范
| 类型 | 规范 | 示例 |
|------|------|------|
| 模块名 | 名词，简洁 | 用户、订单、商品 |
| 子模块 | 动词+名词 | 用户管理、订单查询 |
| 层级 | 不超过3层 | 用户 → 用户管理 → 用户信息 |

### 2. 模块划分原则
- **单一职责**：每个模块只负责一个功能领域
- **高内聚**：模块内部功能紧密相关
- **低耦合**：模块之间依赖最小化
- **可独立部署**：模块可独立测试和部署

### 3. 模块结构示例
```
用户模块 (id: uuid-1, parent: None)
├── 用户管理 (id: uuid-2, parent: uuid-1)
│   ├── 用户信息 (id: uuid-3, parent: uuid-2)
│   └── 用户认证 (id: uuid-4, parent: uuid-2)
└── 用户统计 (id: uuid-5, parent: uuid-1)
```

### ⚡ 粒度要求
- 中粒度：描述模块结构和职责
- 不需要深入到字段或接口
- 关注模块划分和依赖关系
- 示例：描述"用户管理模块包含用户信息和用户认证"而不需要描述"用户信息有几个字段"